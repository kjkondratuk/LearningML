"""Training loop for RL capstone."""

def train_agent(env_name: str, total_timesteps: int = 1000000, seed: int = 0) -> dict:
    """Train PPO agent and return metrics."""
    raise NotImplementedError
