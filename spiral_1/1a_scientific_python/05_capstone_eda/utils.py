"""Capstone EDA — Reusable Utilities

As you work through the capstone EDA, extract reusable functions here.
This file starts empty. You populate it.

Suggested functions to build:
- load_housing_data() — load and do initial type cleanup
- plot_with_style() — your preferred figure defaults
- summarize_missing() — missing value report for a DataFrame
- top_correlations() — extract and sort correlations with a target column
"""
